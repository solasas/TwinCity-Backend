package com.sashank.DigitalTwinBackend.congestion;

/** Output of {@link RoadCongestionHeuristic} — a rule-based bucket, not a model prediction. */
public enum CongestionLevel {
    LOW, MEDIUM, HIGH
}
